5. 选项

如果没有指定，数值选项接受数字作为输入的时候，遵循SI单位的前缀，例如：'K'，'M'，'G'。如果SI单位的前面加“i”，整个前缀按照二进制的乘方计算，也就是1024进位，而不是1000.

SI单位的前缀追加'B'的，乘以8。这允许使用，例如：'KB'，'MiB'，'G'和'B'作为数量后缀。

不带参数的选项是布尔选项，并设置相应的值为true。可以通过与“no”的选项设置为false。例如使用“-nofoo”将设置“foo”为False选项。
