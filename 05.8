5.8 高级音频选项:

‘-atag fourcc/tag (output)’
强制音频 标签／四字符代码。 这是'-tag:a'的一个别名。

‘-absf bitstream_filter’
已废弃, 见 -bsf

‘-guess_layout_max channels (input,per-stream)’
若音频输入的声道布局未知，将尝试猜测与其对应的最多声道数量。例如，2告诉ffmpeg将一个声道的识别为单声道，将两个声道的识别为立体声，但5.1环绕声并不会被当做六个声道来识别。默认动作为总是猜测。使用'0'来禁用全部猜测。
