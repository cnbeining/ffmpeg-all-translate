5.8 进阶音频选项:

‘-atag fourcc/tag (output)’
强制音频 标签／四字符代码。 这是'-tag:a'的一个别名。

‘-absf bitstream_filter’
已废弃, 见 -bsf

‘-guess_layout_max channels (input,per-stream)’
若输入的声道布局未知，将尝试猜测与其对应的最多声道数量。例如，2告诉ffmpeg将单通道识别为单声道，将双通道识别为立体声，但5.1环绕声并不会被识别成六通道。默认行为为总是猜测。使用'0'来禁用全部猜测。
